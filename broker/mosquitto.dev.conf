# ─── usp-airline — Mosquitto MQTT Broker Config (Modo Dev) ────────────

# Listener MQTT padrão (aviões e servidor de aplicação)
listener 1883
protocol mqtt

# Listener WebSocket (browser pode conectar diretamente se necessário)
listener 9001
protocol websockets

# Sem autenticação (desenvolvimento) — em produção: password_file
allow_anonymous true

# Persistência: retained messages sobrevivem ao restart do broker
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
log_timestamp true

# Qualidade de serviço máxima permitida
max_qos 1

# Tamanho máximo de mensagem (bytes)
max_packet_size 65536

# Fila de mensagens por cliente
max_inflight_messages 100
max_queued_messages 1000

# OBS: Bloco de Bridges removido neste arquivo para evitar erros de DNS no modo de desenvolvimento.